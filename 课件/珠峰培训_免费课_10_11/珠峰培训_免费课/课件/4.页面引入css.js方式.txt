1.页面引入css方式
    1.行内引入(不推荐)
    2.内嵌样式
    3.外链样式
    4.@import "";


2.引入js方式
    1.行内引入JS(不推荐)
    2.内嵌JS代码
    3.外链JS文件（外链引入js不会执行script内部代码）
    4.动态引入
    5.两个延迟属性：async defer（扩展知识）

3.CSS,JS位置：
    1.CSS：图纸 ，JS：加工（动态效果、交互）
    2.CSS放在上面，JS放在下面
    3.为什么CSS放在上面，JS放在下面

